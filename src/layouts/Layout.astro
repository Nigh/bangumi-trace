---
import { ClientRouter } from 'astro:transitions';
import { pwaInfo } from "virtual:pwa-info"
import ReloadPrompt from "../components/ReloadPrompt.svelte"
import "../styles/global.css"

interface Props {
	title: string
}

const { title } = Astro.props
const baseUrl = import.meta.env.BASE_URL
const assetBaseUrl = baseUrl.replace(/\/$/, "")
---

<!doctype html>
<html lang="zh-CN" data-theme="xianii">
	<head>
		<meta charset="UTF-8" />
		<meta name="description" content="Personal anime watch history" />
		<meta name="viewport" content="width=device-width, initial-scale=1" />
		<link rel="icon" type="image/png" href={`${assetBaseUrl}/favicon.png`} />
		<meta name="generator" content={Astro.generator} />
		<title>{title}</title>
		<script is:inline>
			// Apply saved theme before paint to avoid flash
			(() => {
				const saved = localStorage.getItem("theme");
				const theme =
					saved === "xianii" || saved === "xianii-light"
						? saved
						: saved === "xianii-dark"
							? "xianii"
							: null;
				if (theme) document.documentElement.setAttribute("data-theme", theme);
			})();
		</script>
		<ClientRouter />
		{pwaInfo && <Fragment set:html={pwaInfo.webManifest.linkTag} />}
	</head>
	<body>
		<slot />
		<ReloadPrompt client:load />
	</body>
</html>
